# 🔧 Инструкция по интеграции v4.2

## Шаг 1: Подготовка

Файл `api/webhook-v4.2.js` уже создан как копия `api/webhook-v4-simple.js`.

## Шаг 2: Добавить в начало файла (после строки 11)

```javascript
// User statistics (in-memory)
const userStats = new Map();

// Language names for translation
const langNames = {
  en: 'английский', ru: 'русский', es: 'испанский',
  de: 'немецкий', fr: 'французский', it: 'итальянский',
  pt: 'португальский', zh: 'китайский', ja: 'японский', ko: 'корейский'
};
```

## Шаг 3: Добавить функции (после функции generateContent, перед export default)

Добавить 5 новых функций:
1. `updateStats(userId, action)`
2. `translateText(text, targetLang)`
3. `improveText(text)`
4. `brainstormIdeas(topic)`
5. `explainSimply(topic)`

## Шаг 4: Добавить команды в handler

После команды `/generate` добавить 5 новых команд:
1. `/translate`
2. `/improve`
3. `/brainstorm`
4. `/explain`
5. `/stats`

## Шаг 5: Обновить /start сообщение

Добавить новые команды в список.

## Шаг 6: Добавить updateStats во все обработчики

В каждый обработчик команды добавить вызов `updateStats(userId, '/command')`.

---

## Готовый код для копирования

Я создам полный файл `api/webhook-v4.2-complete.js` с всеми изменениями.

**Статус:** Готов к применению  
**Версия:** 4.2  
**Время:** ~5 минут на применение
